import { renderComponent , expect } from '../test_helper';
import AddCard from '../../src/containers/add-card';

describe('Add Card', () => {

	let component;

	// TODO: need to render this.props.deck.title in test!
	beforeEach( () => {
		// component = renderComponent(AddCard);
	});

});
